apiVersion: apps/v1
kind: Deployment
metadata:
  name: tempconv-grpc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tempconv-grpc
  template:
    metadata:
      labels:
        app: tempconv-grpc
    spec:
      containers:
        - name: tempconv-grpc
          image: us-central1-docker.pkg.dev/poised-space-485913-h8/tempconv-repo/tempconv:latest
          ports:
            - containerPort: 50051
          env:
            - name: PORT
              value: "50051"
